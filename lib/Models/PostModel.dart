class PostModel {
  late String postImage,
      postDescription,
      likeCounts,
      userImg,
      userName,
      userLocation,
      postTime;

  PostModel(this.postImage, this.postDescription, this.likeCounts, this.userImg,
      this.userName, this.userLocation, this.postTime);
}
